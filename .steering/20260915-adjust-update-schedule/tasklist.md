# tasklist.md — スケジュール変更の実装タスク

## 1. ワークフロー変更
- [x] `.github/workflows/update-data.yml` の `on.schedule` を5本のcron式に置き換え
- [x] 各cron式にJST換算のコメントを付記
- [x] コメント（先頭の説明行）も新しいスケジュールに合わせて更新

## 2. 確認
- [x] cron式が前場(9:00-11:30)・後場(12:30-15:30)の15分おき、それ以外の
      時間帯（昼休み・場外）を含まないことを目視確認
- [x] `workflow_dispatch` が残っていることを確認
- [x] YAML構文が正しいことを確認（インデント・リスト形式）

## 完了条件
- 平日の前場・後場のみ、15分おきに `update-data.yml` が実行されるスケジュールになっている
- 既存のジョブ内容（データ取得・コミット）は変更されていない
